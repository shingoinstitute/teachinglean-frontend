import { ForumDetailDirective } from './forum-detail.directive';

describe('ForumDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ForumDetailDirective();
    expect(directive).toBeTruthy();
  });
});
